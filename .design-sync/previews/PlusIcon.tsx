import { PlusIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'var(--brand-primary)', padding: 24, borderRadius: '50%', color: 'var(--bg-0)', display: 'inline-flex' }}>
      <PlusIcon size={28} />
    </div>
  )
}
