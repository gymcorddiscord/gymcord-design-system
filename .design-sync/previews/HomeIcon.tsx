import { HomeIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'var(--bg-1)', padding: 24, borderRadius: 12, color: 'var(--text-secondary)', display: 'inline-flex' }}>
      <HomeIcon size={32} />
    </div>
  )
}
