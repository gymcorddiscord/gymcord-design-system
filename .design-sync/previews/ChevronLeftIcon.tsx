import { ChevronLeftIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'var(--bg-2)', padding: 24, borderRadius: '50%', color: 'var(--text-secondary)', display: 'inline-flex' }}>
      <ChevronLeftIcon size={28} />
    </div>
  )
}
