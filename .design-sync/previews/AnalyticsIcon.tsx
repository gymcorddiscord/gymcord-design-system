import { AnalyticsIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'var(--spectrum-blue)', padding: 24, borderRadius: 12, color: 'var(--text-primary)', display: 'inline-flex' }}>
      <AnalyticsIcon size={32} />
    </div>
  )
}
