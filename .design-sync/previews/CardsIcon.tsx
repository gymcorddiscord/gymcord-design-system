import { CardsIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'var(--bg-2)', padding: 24, borderRadius: 12, color: 'var(--text-secondary)', display: 'inline-flex' }}>
      <CardsIcon size={32} />
    </div>
  )
}
