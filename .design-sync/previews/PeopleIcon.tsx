import { PeopleIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'var(--brand-primary)', padding: 24, borderRadius: 12, color: 'var(--bg-0)', display: 'inline-flex' }}>
      <PeopleIcon size={32} />
    </div>
  )
}
