import { MoonIcon } from 'gymcord-design-system'

export function Default() {
  return (
    <div style={{ background: 'rgba(10,14,20,0.55)', padding: 24, borderRadius: 12, color: 'var(--text-primary)', display: 'inline-flex' }}>
      <MoonIcon size={32} />
    </div>
  )
}
